var subir_bajar = prompt("subes o bajas");
var pisoActual = 1;
const primerPiso = 2;
const ultimoPiso = 20;

for (subir_bajar == "subo") {
	var pisoSeleccionado = prompt("a que piso quieres ir");
	 for (subir_bajar; pisoActual <= pisoSeleccionado; pisoActual++ ) {
	 	alert("llegaste");
	 }
}